services:
  - type: worker
    name: instagram-downloader-bot
    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      python app.py

    envVars:
      - key: BOT_TOKEN
        sync: false
